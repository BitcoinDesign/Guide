services:
  jekyll:
    image: jekyll/jekyll:4.2.2
    ports:
      - '4000:4000'
    volumes:
      - '.:/srv/jekyll'
    command: >
      sh -c 'bundle install &&
             jekyll serve --host 0.0.0.0 --force_polling --watch'
